import {request} from '../utils/request';import type {Journal,JournalPayload} from '../types';export const listJournals=(level?:number)=>request<Journal[]>(`/journals${level?`?mood_level=${level}`:''}`);export const saveJournal=(payload:JournalPayload)=>request<Journal>('/journals',{method:'POST',body:JSON.stringify(payload)});export const updateJournal=(id:number,payload:JournalPayload)=>request<Journal>(`/journals/${id}`,{method:'PUT',body:JSON.stringify(payload)});export const deleteJournal=(id:number)=>request(`/journals/${id}`,{method:'DELETE'});
